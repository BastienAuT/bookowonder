{% extends 'base.html.twig' %}

{% block title %}Favorite{% endblock %}

{% block body %}
    <h1>Favorite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ favorite.id }}</td>
            </tr>
            <tr>
                <th>IsFavorite</th>
                <td>{{ favorite.isFavorite ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_favorite_browse') }}">back to list</a>

    <a href="{{ path('admin_favorite_edit', {'id': favorite.id}) }}">edit</a>

    {{ include('admin/favorite/_delete_form.html.twig') }}
{% endblock %}
