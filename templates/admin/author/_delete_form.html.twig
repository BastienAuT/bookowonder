<form method="post" action="{{ path('admin_author_delete', {'id': author.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet auteur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ author.id) }}">
    <button class="btn-test bgCancelshred">Supprimer l'auteur</button>
</form>
