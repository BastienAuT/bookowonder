<form method="post" action="{{ path('admin_recommendation_delete', {'id': recommendation.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cette recommandation ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recommendation.id) }}">
    <button class="btn-test bgCancelshred">Supprimer la recommandation</button>
</form>
